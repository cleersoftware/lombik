[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lombik"
version = "0.1.0"
dependencies = [
    "click"
]

[project.scripts]
lombik = "lombik.cli:cli"